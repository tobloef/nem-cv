export default class ${NAME} extends BaseComponent {
    static observedAttributes = [

    ];

    usedComponents = [

    ];

    // language=HTML
    get html() {
        return `
            
        `;
    }

    script = () => {

    };

    externalStyles = [

    ];

    // language=CSS
    get style() {
        return `
            
        `
    };
}
