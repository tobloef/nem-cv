export default class ${NAME}
    extends BaseComponent {
    usedComponents = [];

    // language=HTML
    get html() {
        return `<div id="root"></div>`;
    };

    script = () => {

    };

    // language=CSS
    style = `
        #root {

        }
    `;
}
