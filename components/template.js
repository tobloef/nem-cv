export default class ${NAME} extends BaseComponent {
    usedComponents = [

    ];

    // language=HTML
    html = `
        <div id="root"></div>
    `;

    script = () => {

    };

    // language=CSS
    style = `
        #root {
        
        }
    `;
}
